<!DOCTYPE html>
<html>
<head>
  <title>Bupper</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <link rel="stylesheet" href="https://api.mapbox.com/mapbox-gl-js/v3.15.0/mapbox-gl.css">
  <%= javascript_importmap_tags %>
</head>
<body class="dark min-h-screen bg-gradient-to-br from-[#0D1117] to-[#1C1F26] text-slate-200">
  <nav class="border-b border-[#2E3440] bg-slate-900/80 backdrop-blur">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <a href="/" class="text-2xl font-bold text-cyan-300">Bupper</a>
          </div>
          <div class="hidden md:block">
            <div class="ml-10 flex items-baseline space-x-4">
              <%= link_to "Dashboard", root_path, class: "text-slate-400 hover:text-slate-100 px-3 py-2 text-sm font-medium transition" %>
              <%= link_to "Jobs", jobs_path, class: "text-slate-400 hover:text-slate-100 px-3 py-2 text-sm font-medium transition" %>
              <%= link_to "Benchmarks", benchmarks_path, class: "text-slate-400 hover:text-slate-100 px-3 py-2 text-sm font-medium transition" %>
              <%= link_to "Settings", "#", class: "text-slate-400 hover:text-slate-100 px-3 py-2 text-sm font-medium transition" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <main>
    <div>
      <% flash.each do |type, message| %>
        <div class="mb-4 rounded-md border border-[#2E3440] bg-slate-900/70 px-4 py-3 text-sm text-slate-100 shadow">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </main>
</body>
</html>
